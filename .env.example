# DinoRank Proxy Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# SECRET KEY (Required for HMAC token signing)
# =============================================================================
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# NEVER share this key or commit it to public repositories
SECRET_KEY=your_64_character_hex_secret_key_here

# =============================================================================
# Proxy Port
# =============================================================================
PORT_DINORANK=4040

# =============================================================================
# DinoRank Credentials (for automatic cookie extraction)
# =============================================================================
# Note: Automatic login doesn't work due to captcha.
# Extract cookies manually from browser instead (see README.md)
DINORANK_EMAIL=your_email@example.com
DINORANK_PASSWORD=your_password_here

# =============================================================================
# DinoRank Session Cookies
# =============================================================================
# Extract these manually from your browser after logging in:
#
# Chrome/Edge: F12 > Application > Cookies > dinorank.com > Right-click > "Copy all as Header String"
# Firefox: F12 > Storage > Cookies > https://dinorank.com > Select all > Copy
#
# Format: cookie1=value1; cookie2=value2; cookie3=value3
#
# You can use either variable name:
OPENID=_ga=GA1.2.xxx; _gid=GA1.2.yyy; PHPSESSID=zzz; userId=123; other_cookies=here
# or
# MASTER_COOKIES=_ga=GA1.2.xxx; _gid=GA1.2.yyy; PHPSESSID=zzz; userId=123; other_cookies=here

# =============================================================================
# CORS Allowed Origins (optional)
# =============================================================================
# Comma-separated list of domains allowed to make requests to the proxy
ALLOWED_ORIGINS=https://dinorank.seoconjunta.net,https://seoconjuntas.net

# =============================================================================
# Upstream Proxy (optional)
# =============================================================================
# Route traffic through another proxy if needed
# UPSTREAM_PROXY=http://proxy.example.com:8080
# UPSTREAM_PROXY=socks5://proxy.example.com:1080

# =============================================================================
# Session Keepalive Configuration
# =============================================================================
# Periodic requests to prevent cookie expiration between renewal cycles
KEEPALIVE_ENABLED=true

# Interval between pings (in hours)
# - Cookies expire every 30 min: use 0.25 (15 min safety margin)
# - Cookies last 6+ hours: use 4.5 (4.5 hours)
KEEPALIVE_INTERVAL_HOURS=0.25

# Endpoint to ping to keep session alive
# /homed/ is the dashboard - lightweight and has no side effects
KEEPALIVE_ENDPOINT=/homed/
